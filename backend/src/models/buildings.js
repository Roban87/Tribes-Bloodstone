export const buildings = {
  id: buildingsData.id,
  type: buildingsData.type,
  level: buildingsData.level,
  hp: buildingsData.hp,
  started_at: buildingsData.started_at,
  finished_at: buildingsData.finished_at,
};
